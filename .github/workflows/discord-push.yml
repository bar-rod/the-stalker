name: Discord Notification
on:
  push:
    branches:
    - main

jobs:
  send-discord:
    runs-on: ubuntu-latest
    steps:
    - name: Send Discord message
      run: |
        curl -H "Content-Type: application/json" \-X POST \
        -d "{"content": "🎉 New push to **${{ github.repository }}** on branch **${{ github.ref_name }}** by **${{ github.actor }}**! Commit message: '${{ github.event.head_commit.message }}'"}" \${{ secrets.DISCORD_WEBHOOK }}
